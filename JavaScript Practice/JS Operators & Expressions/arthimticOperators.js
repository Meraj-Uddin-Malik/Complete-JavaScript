// JavaScript operators are special symbols or keywords used to perform operations on values and variables. They allow you to manipulate data, compare values, assign values, and perform logical operations. Common types include:

// - Arithmetic operators (e.g., +, -, *, /)
// - Comparison operators (e.g., ==, ===, >, <)
// - Logical operators (e.g., &&, ||, !)
// - Assignment operators (e.g., =, +=, -=)
// - Other operators like typeof, instanceof

// They are fundamental to writing expressions and controlling program logic.

console.log("Operators In JavaScript");


console.log("Arthimatic Opertor");

let a = 10;
let b = 5;

console.log("a + b =", a + b); // a + b = 15
console.log("a - b =", a - b); // a - b = 5
console.log("a * b =", a * b); // a * b = 50
console.log("a / b =", a / b); // a / b = 2
console.log("a % b =", a % b); // a % b = 0

console.log("a++ =", a++); // a++ = 10 (prints then increments)
console.log("a-- =", a--); // a-- = 11 (prints then decrements)
console.log("++a =", ++a); // ++a = 11 (increments then prints)
console.log("--a =", --a); // --a = 9  (decrements then prints)
